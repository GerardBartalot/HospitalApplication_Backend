name: Java CI with Maven

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      # Cambiar al directorio correcto
      - name: Set working directory
        run: cd M13-Spring-Boot

      # Configurar el JDK
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven

      # Construir con Maven
      - name: Build with Maven
        run: mvn -B package
        working-directory: M13-Spring-Boot

      # Ejecutar pruebas
      - name: Run tests
        run: mvn test
        working-directory: M13-Spring-Boot
